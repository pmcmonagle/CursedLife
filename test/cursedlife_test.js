'use strict';

var cursedlife = require('../');
var assert = require('should');

describe('cursedlife', function () {

  it('should be awesome', function () {
    cursedlife().should.equal('awesome');
  });

});
